# Used at build time. Leave empty to call /api on the same Vercel origin
# (see vercel.json rewrites). Override in the Vercel dashboard if needed.
VITE_API_URL=https://rct-folder-management-system-eqbo.onrender.com
